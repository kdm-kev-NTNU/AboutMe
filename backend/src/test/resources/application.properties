# WebMvc slices share a cached context; rate-limit filters use in-memory buckets and count every
# matching POST (including 4xx). Disable for most tests; enable only in *RateLimitFilterTest classes.
portfolio.ask-rate-limit.enabled=false
portfolio.feedback-rate-limit.enabled=false
