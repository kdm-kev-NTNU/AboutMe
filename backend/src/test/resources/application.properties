# WebMvc slices share a cached context; the /ask rate-limit filter uses in-memory buckets and
# counts every POST /ask (including 400s). Disable for most tests; enable only in RateLimitFilterTest.
portfolio.ask-rate-limit.enabled=false
